pipeline {
    agent any

    environment {
        // Перенаправляем домашнюю директорию Go в папку сборки Jenkins
        HOME = "${WORKSPACE}"
        GOCACHE = "${WORKSPACE}/.cache/go-build"
    }

    stages { 
        stage('Unit Tests - Backend') {
            agent {
                docker {
                    image 'snakee/golang-junit:1.21'
                    reuseNode true
                }
            }
            steps {
                dir('bugtracker-backend') {
                    sh 'go test -v ./...'
                }
            }
        }
    }
}